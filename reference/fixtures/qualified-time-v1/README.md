# Qualified-time synthetic fixture

All facts and identifiers in `pending-amendment.json` are invented and redistributable under this repository's reference-material license. No restricted adopter corpus is copied here.

The fixture starts with unresolved commencement and retains expected and fallback dates. The mutation suite exercises both branches and the evidence boundaries defined in [the contract](../../contracts/qualified-time-fixture-v1.md). Colorado source-grounded acceptance cases remain in the owning adopter repositories.

Run the synthetic checks from the OF repository root.

Literal
```bash
node scripts/test-qualified-time.mjs
```
